<?xml version="1.0" encoding="utf-8"?>
<!-- S12 · 참여자 1명 체크리스트: 탭 없이 내 목록만 단독으로 표시한다. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bag_bg_base">

    <LinearLayout
        android:id="@+id/checklistSoloContent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="@dimen/screen_margin"
        android:paddingTop="@dimen/space_lg"
        android:paddingEnd="@dimen/screen_margin">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/checklist_title"
            android:textAppearance="@style/TextAppearance.Bag.DisplayM"
            android:textStyle="bold" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_2xs"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/checklistSoloTripName"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="@dimen/space_md"
                android:layout_weight="1"
                android:ellipsize="end"
                android:maxLines="1"
                tools:text="@string/checklist_trip_name"
                android:textAppearance="@style/TextAppearance.Bag.BodyM" />

            <TextView
                android:id="@+id/checklistSoloProgressLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:maxLines="1"
                tools:text="@string/checklist_solo_progress_summary"
                android:textAppearance="@style/TextAppearance.Bag.BodyMMedium" />
        </LinearLayout>

        <ProgressBar
            android:id="@+id/checklistSoloProgressBar"
            style="@style/Widget.Bag.ProgressBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_sm"
            android:max="100"
            android:progress="100" />
    </LinearLayout>

    <FrameLayout
        android:id="@+id/checklistSoloFragmentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    </LinearLayout>

    <FrameLayout
        android:id="@+id/checklistSoloLoadingOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/bag_loading_scrim"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone">

        <ProgressBar
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/bag_brand_default" />
    </FrameLayout>

</FrameLayout>
